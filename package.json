{
  "name": "more-testable-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-server": "rm -rf dist && babel src --copy-files --out-dir dist",
    "build": "npm run build-server",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [],
  "author": "Adam Johnston",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0",
    "googleapis": "^25.0.0",
    "hapi": "^17.2.0",
    "lodash": "^4.17.4",
    "rxjs": "^5.5.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "chai": "^4.1.2",
    "mocha": "^5.0.0"
  },
  "babel": {
    "plugins": [
      "transform-object-rest-spread"
    ],
    "presets": [
      "env"
    ]
  }
}
